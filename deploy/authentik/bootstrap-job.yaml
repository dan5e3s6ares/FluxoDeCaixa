apiVersion: batch/v1
kind: Job
metadata:
  name: authentik-bootstrap
  labels:
    app.kubernetes.io/name: authentik-bootstrap
    app.kubernetes.io/part-of: fluxo-caixa
spec:
  ttlSecondsAfterFinished: 300
  activeDeadlineSeconds: 900
  backoffLimit: 6
  template:
    metadata:
      labels:
        app.kubernetes.io/name: authentik-bootstrap
    spec:
      restartPolicy: OnFailure
      containers:
        - name: bootstrap
          image: curlimages/curl:8.12.1
          command: ["/bin/sh", "/scripts/bootstrap.sh"]
          env:
            - name: AUTHENTIK_URL
              value: http://authentik-server.security.svc.cluster.local:9000
            - name: APP_SLUG
              value: fluxo-caixa
            - name: AUTHENTIK_BOOTSTRAP_TOKEN
              valueFrom:
                secretKeyRef:
                  name: fluxo-authentik
                  key: AUTHENTIK_BOOTSTRAP_TOKEN
            - name: AUTHENTIK_WAIT_ATTEMPTS
              value: "90"
            - name: AUTHENTIK_WAIT_DELAY
              value: "5"
            - name: JWKS_WAIT_ATTEMPTS
              value: "90"
            - name: JWKS_WAIT_DELAY
              value: "5"
            - name: OAUTH2_DEFAULTS_WAIT_ATTEMPTS
              value: "30"
            - name: OAUTH2_DEFAULTS_WAIT_DELAY
              value: "2"
          volumeMounts:
            - name: scripts
              mountPath: /scripts
              readOnly: true
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        - name: scripts
          configMap:
            name: authentik-bootstrap
            defaultMode: 0555
