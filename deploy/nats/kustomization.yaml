apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: messaging

configMapGenerator:
  - name: nats-bootstrap
    files:
      - bootstrap.sh=../../platform/nats/bootstrap.sh
    options:
      disableNameSuffixHash: true

resources:
  - bootstrap-job.yaml
