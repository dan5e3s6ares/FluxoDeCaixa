apiVersion: v1
kind: Service
metadata:
  name: svc-consulta
  labels:
    app.kubernetes.io/name: svc-consulta
    app.kubernetes.io/part-of: fluxo-caixa
spec:
  selector:
    app: svc-consulta
  ports:
    - name: http
      port: 8000
      targetPort: http
