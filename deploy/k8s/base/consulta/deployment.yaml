# svc-consulta — read-only API (doc 07): consolidado/lancamentos queries, Redis cache
apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-consulta
  labels:
    app.kubernetes.io/name: svc-consulta
    app.kubernetes.io/part-of: fluxo-caixa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-consulta
  template:
    metadata:
      labels:
        app: svc-consulta
        app.kubernetes.io/name: svc-consulta
        app.kubernetes.io/part-of: fluxo-caixa
    spec:
      containers:
        - name: consulta
          image: fluxo-caixa/consulta:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: svc-consulta-connection
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: fluxo-pg-app
                  key: database-url-consolidado
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: fluxo-redis
                  key: redis-url
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
