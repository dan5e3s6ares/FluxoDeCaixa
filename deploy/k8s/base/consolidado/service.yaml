apiVersion: v1
kind: Service
metadata:
  name: svc-consolidado
  labels:
    app.kubernetes.io/name: svc-consolidado
    app.kubernetes.io/part-of: fluxo-caixa
spec:
  selector:
    app: svc-consolidado
  ports:
    - name: http
      port: 8000
      targetPort: http
