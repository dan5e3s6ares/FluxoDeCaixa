# svc-consolidado — FastAPI + JetStream consumer (doc 07)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-consolidado
  labels:
    app.kubernetes.io/name: svc-consolidado
    app.kubernetes.io/part-of: fluxo-caixa
spec:
  selector:
    matchLabels:
      app: svc-consolidado
  template:
    metadata:
      labels:
        app: svc-consolidado
        app.kubernetes.io/name: svc-consolidado
        app.kubernetes.io/part-of: fluxo-caixa
    spec:
      containers:
        - name: consolidado
          image: fluxo-caixa/consolidado:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: svc-consolidado-connection
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: fluxo-pg-app
                  key: database-url-consolidado
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: fluxo-redis
                  key: redis-url
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
