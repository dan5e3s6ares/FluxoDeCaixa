# Non-sensitive env for svc-lancamentos (doc 07). Secrets: fluxo-pg-app, fluxo-redis.
apiVersion: v1
kind: ConfigMap
metadata:
  name: svc-lancamentos-connection
  labels:
    app.kubernetes.io/name: svc-lancamentos
    app.kubernetes.io/part-of: fluxo-caixa
data:
  NATS_URL: nats://nats.messaging.svc.cluster.local:4222
  # Authentik OIDC stubs — filled by follow-up deploy tasks
  OIDC_ISSUER: http://authentik.security.svc.cluster.local/application/o/fluxo-caixa/
  OIDC_CLIENT_ID: svc-lancamentos
  OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector.observability.svc.cluster.local:4317
  OTEL_SERVICE_NAME: svc-lancamentos
  LOG_LEVEL: INFO
  ENVIRONMENT: dev
